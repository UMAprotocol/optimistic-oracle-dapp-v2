import Settled from "@/components/pages/settled";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Optimistic Oracle V2 | Settled",
};

export default function SettledPage() {
  return <Settled />;
}
